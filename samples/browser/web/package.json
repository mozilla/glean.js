{
  "name": "web",
  "version": "1.0.0",
  "type": "module",
  "description": "A sample website using Glean.js",
  "scripts": {
    "link:glean": "cd ../../../glean && npm i && npm run build && npm link && cd ../samples/browser/web && npm link @mozilla/glean",
    "glean": "glean translate src/metrics.yaml src/pings.yaml -f javascript -o src/generated",
    "build": "npm run glean && webpack ./src/index.js --output-filename=index.js --mode=production",
    "dev": "webpack ./src/index.js --output-filename=index.js --mode=development --watch"
  },
  "author": "The Glean Team <glean-team@mozilla.com>",
  "license": "MPL-2.0",
  "devDependencies": {
    "webpack": "^5.58.1",
    "webpack-cli": "^4.9.1",
    "mocha": "^9.1.1"
  }
}
